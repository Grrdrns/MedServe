{
  "name": "bukmd-patient-portal",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev -w frontend",
    "build": "npm run build -w frontend",
    "backend:dev": "npm run dev -w backend",
    "install:all": "npm install && npm install -w frontend && npm install -w backend"
  }
}
